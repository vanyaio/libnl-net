#include<string.h>
#include<stdio.h>

int main(int argc, char* argv[]){
  /*
  sleep(5);
  char buf[100];
  strcpy(buf, "php -S ");
  strcat(buf, argv[2]);
  strcat(buf, ":8080");
  //strcat(buf, " > /dev/null 2> /dev/null");
  system(buf);
  */
  exit(0);
}
