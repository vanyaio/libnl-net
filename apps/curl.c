#include<stdio.h>

int main(int argc, char* argv[]){
  //sleep(20);
  /*
  printf("starting curl\n");
  system("rm -rf apps/tmp");
  system("mkdir apps/tmp");
  system("cp -r /etc/* apps/tmp");
  system("mount --bind apps/tmp /etc");

  system("echo \"127.0.0.1	localhost\" > /etc/hosts");
  system("echo \"10.0.0.2	curl\" >> /etc/hosts");
  system("echo \"10.0.0.3	serv1\" >> /etc/hosts");
  system("echo \"10.0.0.4 serv2\" >> /etc/hosts");

  system("/bin/sh");
  */
  exit(0);
}
